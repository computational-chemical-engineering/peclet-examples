"""Placeholder package for the peclet example gallery.

Examples are deliberately **self-contained** — each notebook creates its own
geometry, sets all parameters, and defines any helper functions inline, so it runs
top-to-bottom on Colab with nothing but `pip install peclet`. This package exists
only so the repo is installable (and as a home for shared literature-reference
datasets / plotting styles, should the gallery grow to need them).
"""

__version__ = "0.1.0"
