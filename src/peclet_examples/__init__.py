"""Helper code shared across the peclet example gallery.

Examples import from this installed package (``pip install -e .``) rather than
using fragile ``sys.path`` hacks, so the same code runs locally, in CI, and on
Binder/Colab.
"""

__all__ = ["mms", "channels"]
